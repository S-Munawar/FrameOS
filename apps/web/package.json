{
  "name": "@frameos/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Web app placeholder'",
    "build": "echo 'Web app placeholder'",
    "test": "echo 'Web app placeholder'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@frameos/runtime": "workspace:*",
    "@frameos/types": "workspace:*"
  },
  "devDependencies": {
    "@frameos/config": "workspace:*",
    "typescript": "^5.3.0"
  }
}
